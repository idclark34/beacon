{
  "name": "outpost-companion",
  "version": "1.0.0",
  "description": "Local-first AI character for solo building sessions",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "dev": "NODE_ENV=development electron .",
    "postinstall": "electron-rebuild -f -w better-sqlite3 --build-from-source --arch arm64",
    "rebuild": "electron-rebuild -f -w better-sqlite3 --build-from-source --arch arm64"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.40.0",
    "better-sqlite3": "^12.6.2",
    "chokidar": "^3.6.0",
    "dotenv": "^16.4.5",
    "rss-parser": "^3.13.0",
    "simple-git": "^3.22.0"
  },
  "devDependencies": {
    "electron": "^33.4.11",
    "electron-rebuild": "^3.2.9"
  }
}
